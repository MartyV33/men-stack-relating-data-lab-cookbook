<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Item</title>
</head>
<body>
  <%- include('../partials/nav') %>
  <h1>Edit Item</h1>
  <form action="/users/<%= userId %>/foods/<%= foodItem._id %>?_method=PUT" method="POST">
    <label for="name">Item Name:</label>
    <input type="text" name="name" id="name" value="<%= foodItem.name %>" required>
    <label for="quantity">Quantity:</label>
    <input type="number" name="quantity" id="quantity" value="<%= foodItem.quantity %>" required>
    <label for="expirationDate">Expiration Date:</label>
    <input type="date" name="expirationDate" id="expirationDate" value="<%= foodItem.expirationDate.toISOString().split('T')[0] %>" required>
    <button type="submit">Update Item</button>
  </form>
</body>
</html>